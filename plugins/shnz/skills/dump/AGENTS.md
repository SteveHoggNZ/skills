# Context Dump (Codex Adapter)

When the user asks to export context, create a context bundle, or dump code for another AI tool, follow the procedure in [core.md](./core.md).

Use shell commands (`find`, `grep`, `rg`) for workspace exploration. If LSP tooling is available, prefer it for tracing symbol references.
